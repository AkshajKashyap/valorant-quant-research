# Derived data

Processed outputs are reproducible and intentionally ignored by Git. Recreate
them from a frozen raw snapshot with the documented command in the Milestone 1
report. They must never be treated as a replacement for the raw source files.
